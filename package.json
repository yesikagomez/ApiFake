{
  "name": "api",
  "version": "0.0.1",
  "dependencies": {
    "faker": "^4.1.0",
    "json-server": "^0.14.2"
  },
  "scripts": {
    "start": "json-server --watch ./db.json --port 3005",
    "now:deploy": "now"
  }
}
